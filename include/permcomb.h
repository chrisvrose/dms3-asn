#define _PERM_COMB_H 1

///nCr
int combu(int n, int r);

///nPr
int permu(int n, int r);

///Note: Returns 1 for negative numbers so it does not error
int factorial(int r);

int powerexp(int number, int power);